[project]
name = "cef-data-fetcher"
version = "1.0.0"
description = "Fetch CEF (Closed-End Fund) data from yfinance and CEFConnect"
requires-python = ">=3.10"
dependencies = [
    "pandas>=2.0.0",
    "requests>=2.31.0",
    "yfinance>=0.2.36",
    "beautifulsoup4>=4.12.0",
]

[tool.uv]
dev-dependencies = []
